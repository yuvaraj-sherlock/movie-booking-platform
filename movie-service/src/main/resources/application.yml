server:
  port: 8081

spring:
  application:
    name: movie-service

  datasource:
    url: jdbc:h2:mem:moviesdb
    driverClassName: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true