server:
  port: 8082

spring:
  application:
    name: theatre-service

  datasource:
    url: jdbc:h2:mem:theatredb
    driverClassName: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update